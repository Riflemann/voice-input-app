# Настройка Whisper.cpp для Voice Input App

## 🎯 Интеграция Whisper (готово)

✅ Интегрировал Whisper.cpp с **автоматической загрузкой моделей**:

1. ✅ Добавлена библиотека `whisper-rs = "0.12"`
2. ✅ Реализована система автозагрузки моделей ([setup.rs](src-tauri/src/utils/setup.rs))
3. ✅ Настройка при запуске приложения — проверка и скачивание модели
4. ✅ Создан Whisper сервис ([whisper.rs](src-tauri/src/recognition/whisper.rs))
5. ✅ Добавлена постобработка текста ([postprocess.rs](src-tauri/src/recognition/postprocess.rs))
6. ✅ Обновлены Tauri команды ([recognition.rs](src-tauri/src/commands/recognition.rs))

## 🔍 Как работает автоматическая загрузка

1. ✅ Добавлен `whisper-rs = "0.12"` в Cargo.toml
2. ✅ Реализована система загрузки моделей ([models.rs](src-tauri/src/recognition/models.rs))
3. ✅ Создан Whisper сервис ([whisper.rs](src-tauri/src/recognition/whisper.rs))
4. ✅ Добавлена постобработка текста ([postprocess.rs](src-tauri/src/recognition/postprocess.rs))
5. ✅ Обновлены Tauri команды ([recognition.rs](src-tauri/src/commands/recognition.rs))

## � Как работает автоматическая загрузка

**При первом запуске приложения:**

1. Проверяется наличие модели Whisper в каталоге моделей:
   - Windows: `%AppData%/voice-input-app/whisper_models/`
   - macOS/Linux: соответствующие каталоги

2. Если модели нет — скачивается модель `base` (141 MB) с HuggingFace

3. После скачивания приложение запускается нормально

4. Последующие запуски работают оффлайн (модель кэширована локально)

**Без интернета/в корпоративной сети:**

Используйте переменную окружения `WHISPER_MODELS_DIR` для указания пути к локальным моделям:

```bash
# Скопируйте ggml-base.bin в /opt/models/ и запустите:
WHISPER_MODELS_DIR=/opt/models npm run tauri dev
```

## 🔧 Установка зависимостей

## 🔧 Установка зависимостей

### Требуется для компиляции Whisper

1. **LLVM** (libclang) — для компиляции whisper-rs
2. **Node.js** (v18+) и **Rust** (stable) — для сборки приложения

### Windows

**Вариант 1: через winget (рекомендуется)**
```powershell
winget install LLVM.LLVM
```

**Вариант 2: ручная установка**
1. Скачайте LLVM: https://github.com/llvm/llvm-project/releases/latest
2. Выберите `LLVM-*-win64.exe`
3. Запустите установщик
4. ✅ Обязательно выберите "Add LLVM to system PATH"

После установки перезапустите PowerShell и проверьте:
```powershell
clang --version
```

### macOS

```bash
brew install llvm
```

Затем добавьте в `~/.zshrc` или `~/.bash_profile`:
```bash
export PATH="/usr/local/opt/llvm/bin:$PATH"
export LDFLAGS="-L/usr/local/opt/llvm/lib"
export CPPFLAGS="-I/usr/local/opt/llvm/include"
```

### Linux (Ubuntu/Debian)

```bash
sudo apt update
sudo apt install llvm-dev libclang-dev clang
```

### Linux (Fedora)

```bash
sudo dnf install llvm-devel clang-devel
```

## 📥 Загрузка моделей

Модели скачиваются автоматически при первом запуске. Если нужна ручная установка, смотрите [docs/WHISPER_MODELS.md](docs/WHISPER_MODELS.md).

## 🚀 Сборка и запуск

После установки LLVM:

```bash
# Проверка компиляции
cargo check --manifest-path src-tauri/Cargo.toml

# Запуск в режиме разработки (модель скачается автоматически)
npm run tauri dev

# Сборка для production (модель скачается при первом запуске)
npm run tauri build
```

**На первом запуске:**
- Приложение проверит наличие модели
- Если модели нет, запустится процесс загрузки (2-5 минут)
- После загрузки приложение станет полностью функциональным

## 💻 Использование в коде

### Frontend (TypeScript)

```typescript
import { invoke } from '@tauri-apps/api/tauri'

// 1. Инициализация модели при запуске приложения
async function initializeWhisper() {
  try {
    await invoke('init_whisper', { 
      modelSize: 'base' // или 'tiny', 'small', 'medium', 'large'
    })
    console.log('Whisper initialized')
  } catch (error) {
    console.error('Failed to initialize Whisper:', error)
  }
}

// 2. Распознавание речи
async function recognizeAudio(audioPath: string) {
  try {
    const text = await invoke<string>('recognize_audio', { 
      audioPath 
    })
    console.log('Recognized:', text)
    return text
  } catch (error) {
    console.error('Recognition failed:', error)
    throw error
  }
}

// 3. Прослушивание событий распознавания
import { listen } from '@tauri-apps/api/event'

listen('recognition-completed', (event) => {
  const { text, audio_path } = event.payload
  console.log('Recognition completed:', text)
})
```

### Backend (Rust)

```rust
use crate::recognition::{whisper, models};

// Инициализация при старте приложения
fn setup_whisper() -> Result<(), String> {
    whisper::init(models::ModelSize::Base)?;
    Ok(())
}

// Распознавание
fn recognize_file(path: &Path) -> Result<String, String> {
    whisper::recognize(path, "ru")
}
```

## 🎛️ Конфигурация

### Директория моделей

По умолчанию модели сохраняются в AppData:
- Windows: `%AppData%/voice-input-app/whisper_models/`
- macOS: `~/Library/Application Support/voice-input-app/whisper_models/`
- Linux: `~/.config/voice-input-app/whisper_models/`

Для переопределения используйте переменную окружения:
```bash
WHISPER_MODELS_DIR=/path/to/models npm run tauri dev
```

### Отключение автозагрузки

Для разработки/офлайн режима:
```bash
SKIP_MODEL_DOWNLOAD=1 npm run tauri dev
```

### Поддерживаемые языки

- `"ru"` - русский
- `"en"` - английский
- `"auto"` - автоматическое определение

Язык передаётся в функцию распознавания. По умолчанию используется русский.

## 🔍 Диагностика проблем

### Ошибка: "couldn't find any valid shared libraries matching: ['clang.dll', 'libclang.dll']"

**Решение:**
1. Убедитесь что LLVM установлен: `clang --version`
2. Перезапустите терминал/IDE после установки
3. Если не помогает, установите переменную окружения:
   ```powershell
   $env:LIBCLANG_PATH = "C:\Program Files\LLVM\bin"
   ```

### Ошибка: "Model file not found" при инициализации

**Решение:**
Это нормально при первом запуске. Приложение автоматически скачает модель.

Если ошибка сохраняется:
1. Проверьте подключение к интернету
2. Убедитесь, что есть 500 MB свободного места на диске
3. Убедитесь, что у пользователя есть права на запись в AppData
4. Посмотрите логи для деталей (запустите с RUST_LOG=debug)

### Ошибка: "Whisper model not initialized"

**Решение:**
Этого не должно быть при нормальном потоке. Если возникает:
1. Проверьте что процесс инициализации завершился успешно (посмотрите логи)
2. Убедитесь что модель была скачана в AppData
3. Проверьте достаточно памяти (минимум 500 MB свободной)

## 📊 Производительность

| Модель | Размер | ~Время на 30 сек аудио | RAM |
|--------|--------|------------------------|-----|
| Tiny   | 75 MB  | 1-2 сек                | ~300 MB |
| Base   | 142 MB | 2-4 сек                | ~500 MB |
| Small  | 466 MB | 5-10 сек               | ~1 GB |
| Medium | 1.5 GB | 15-30 сек              | ~2.5 GB |
| Large  | 3 GB   | 30-60 сек              | ~5 GB |

*Время указано для CPU (Intel i7/AMD Ryzen 7). С GPU будет значительно быстрее.*

## 🎯 Следующие шаги

1. ✅ Установите LLVM
2. ✅ Запустите приложение — модель скачается автоматически
3. ✅ На первом запуске дождитесь завершения загрузки (2-5 минут)
4. ✅ Последующие запуски будут мгновенными (модель кэширована)
5. 🔜 Добавьте UI для выбора других моделей (tiny, small, medium, large)
6. 🔜 Добавьте индикатор прогресса загрузки модели
7. 🔜 Реализуйте кэширование результатов распознавания

## 📚 Дополнительная информация

- [Whisper.cpp GitHub](https://github.com/ggerganov/whisper.cpp)
- [Whisper-rs docs](https://docs.rs/whisper-rs)
- [Модели Whisper](https://huggingface.co/ggerganov/whisper.cpp)
- [OpenAI Whisper](https://github.com/openai/whisper)
