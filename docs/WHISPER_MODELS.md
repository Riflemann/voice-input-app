# Whisper модели и установка

Полное руководство по выбору, установке и использованию моделей Whisper.cpp.

## Оглавление

1. [Быстрый старт](#быстрый-старт)
2. [Доступные модели](#доступные-модели)
3. [Установка моделей](#установка-моделей)
4. [Интеграция в приложение](#интеграция-в-приложение)
5. [Поддерживаемые языки](#поддерживаемые-языки)
6. [Troubleshooting](#troubleshooting)

## Быстрый старт

**Требуемое оборудование:** 
- ОЗУ: минимум 1 ГБ свободной памяти
- SSD: место для модели (39 МБ – 2.9 ГБ)
- CPU: процессор с поддержкой AVX/SSE (последние 10 лет)

**Установка (Windows):**

```powershell
# Создать папку для моделей
mkdir models

# Скачать модель Base (рекомендуется)
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin" -OutFile "models\ggml-base.bin"

# Запустить приложение
npm run tauri dev
```

**Установка (macOS/Linux):**

```bash
mkdir models
curl -o models/ggml-base.bin https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
npm run tauri dev
```

## Доступные модели

Все модели скачиваются с [HuggingFace](https://huggingface.co/ggerganov/whisper.cpp).

| Размер | Размер файла | Скорость | Точность | Рекомендация |
|--------|--------------|----------|----------|--------------|
| **tiny** | 39 МБ | ⚡⚡⚡ | ⭐⭐ | Низкоконечные устройства, требуется скорость |
| **base** | 141 МБ | ⚡⚡ | ⭐⭐⭐ | ✅ **Рекомендуется** — оптимальный баланс |
| **small** | 465 МБ | ⚡ | ⭐⭐⭐⭐ | Улучшенная точность, требуется больше времени |
| **medium** | 1.5 ГБ | ⏱️ | ⭐⭐⭐⭐⭐ | Высокая точность, медленно |
| **large** | 2.9 ГБ | ⏱️⏱️ | ⭐⭐⭐⭐⭐⭐ | Лучшее качество, требует 4+ ГБ ОЗУ |

### Время обработки (примерно)

На Intel i7 8-го поколения:

| Модель | 10 сек аудио | 30 сек аудио |
|--------|--------------|--------------|
| tiny | 0.5 сек | 1.5 сек |
| base | 1.5 сек | 4 сек |
| small | 5 сек | 15 сек |
| medium | 15 сек | 45 сек |
| large | 40 сек | 120 сек |

### Точность на русском языке

Протестировано на русской речи:

| Модель | Точность | Примечание |
|--------|----------|-----------|
| tiny | 60-70% | Только простые фразы |
| base | 85-90% | ✅ Хороший результат для большинства |
| small | 90-95% | Улучшенное распознавание акцентов |
| medium | 92-97% | Отличный результат для чистой речи |
| large | 94-98% | Лучшее качество, но очень медленно |

## Установка моделей

### Вариант 1: Автоматическое скачивание (рекомендуется)

```powershell
# Windows PowerShell
mkdir models
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin" -OutFile "models\ggml-base.bin"
```

```bash
# macOS/Linux
mkdir models
curl -o models/ggml-base.bin https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
```

### Вариант 2: Выбор другой модели

**Замените URL на нужную модель:**

```
https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-{MODEL}.bin
```

Где `{MODEL}` — одно из: `tiny`, `base`, `small`, `medium`, `large`

**Примеры:**

```powershell
# Tiny (быстро, низкое качество)
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin" -OutFile "models\ggml-tiny.bin"

# Small (лучше качество)
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.bin" -OutFile "models\ggml-small.bin"

# Large (лучшее качество, медленно)
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large.bin" -OutFile "models\ggml-large.bin"
```

### Вариант 3: Командная строка git (если установлен Git LFS)

```bash
# Клонирование репозитория (требует Git LFS)
git clone https://huggingface.co/ggerganov/whisper.cpp
cd whisper.cpp
cp ggml-base.bin /path/to/voice-input-app/models/
```

### Вариант 4: Прямое скачивание из браузера

1. Откройте [HuggingFace модели Whisper](https://huggingface.co/ggerganov/whisper.cpp/tree/main)
2. Найдите файл `ggml-{model}.bin`
3. Нажмите кнопку "Download"
4. Поместите файл в папку `models/` проекта

### Проверка скачивания

```powershell
# Проверить наличие файла
Test-Path models\ggml-base.bin

# Проверить размер (должен быть ~141 МБ для base)
(Get-Item models\ggml-base.bin).Length / 1MB
```

## Интеграция в приложение

### Где приложение ищет модель?

**Путь по умолчанию:** `{PROJECT_ROOT}/models/ggml-base.bin`

### Переключение между моделями

**Отредактируйте файл:** [`src-tauri/src/recognition/models.rs`](../src-tauri/src/recognition/models.rs)

```rust
// Найдите эту строку:
const MODEL_PATH: &str = "../models/ggml-base.bin";

// И измените на нужную модель:
const MODEL_PATH: &str = "../models/ggml-tiny.bin";   // Для tiny
const MODEL_PATH: &str = "../models/ggml-small.bin";  // Для small
const MODEL_PATH: &str = "../models/ggml-large.bin";  // Для large
```

**Затем пересборка:**
```bash
npm run tauri dev
```

### Пользовательская папка для моделей

Если хотите сохранять модели в другом месте:

**Файл:** [`src-tauri/src/recognition/models.rs`](../src-tauri/src/recognition/models.rs)

```rust
// Вместо:
const MODEL_PATH: &str = "../models/ggml-base.bin";

// Используйте:
let model_dir = std::path::PathBuf::from(
    std::env::var("WHISPER_MODEL_PATH")
        .unwrap_or_else(|_| "../models".to_string())
);
let model_path = model_dir.join("ggml-base.bin");
```

Затем запустите с переменной окружения:
```powershell
$env:WHISPER_MODEL_PATH = "C:\Models\Whisper"
npm run tauri dev
```

## Поддерживаемые языки

Whisper поддерживает **99 языков**, включая:

| Язык | Код | Точность на base |
|------|-----|-----------------|
| **Русский** 🇷🇺 | `ru` | 85-90% |
| Английский | `en` | 95%+ |
| Испанский | `es` | 90%+ |
| Французский | `fr` | 90%+ |
| Немецкий | `de` | 88%+ |
| Китайский | `zh` | 85%+ |
| Японский | `ja` | 80%+ |
| Корейский | `ko` | 85%+ |
| и другие | [полный список](https://github.com/openai/whisper/blob/main/whisper/tokenizer.py) | 80-95% |

### Автоматическое определение языка

Приложение автоматически определяет язык речи.

Если нужно явно указать язык:

**Файл:** [`src-tauri/src/recognition/whisper.rs`](../src-tauri/src/recognition/whisper.rs)

```rust
// Найдите функцию recognize()
// Добавьте параметр language:

let params = WhisperParams {
    language: Some("ru"),  // Только русский
    // или оставить None для автоопределения
    ..Default::default()
};
```

## Troubleshooting

### Проблема: "Model file not found"

**Решение:**
1. Проверьте, что папка `models/` создана в корне проекта
2. Проверьте имя файла (точно `ggml-base.bin` или выбранная модель)
3. Запустите снова: `npm run tauri dev`

```powershell
# Проверить структуру
ls models
```

### Проблема: "Failed to load model"

**Возможные причины:**
- Файл поврежден при скачивании
- Недостаточно ОЗУ
- Модель не полностью скачана

**Решение:**
1. Удалите файл модели
2. Скачайте заново
3. Проверьте размер:
   - tiny: ~39 МБ
   - base: ~141 МБ
   - small: ~465 МБ
   - medium: ~1.5 ГБ
   - large: ~2.9 ГБ

```powershell
# Удалить и скачать заново
Remove-Item models\ggml-base.bin
Invoke-WebRequest -Uri "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin" -OutFile "models\ggml-base.bin"
```

### Проблема: Распознавание очень медленное

**Причины:**
- Используется слишком большая модель для вашего ПК
- Недостаточно ОЗУ
- CPU использует один поток

**Решение:**
1. Переключитесь на меньшую модель (tiny или base)
2. Закройте другие приложения
3. Убедитесь, что ОЗУ: минимум 2-4 ГБ свободной памяти

### Проблема: Плохая точность на русском языке

**Причины:**
- Используется tiny модель (слишком маленькая)
- Качество записи (см. [AUDIO_PIPELINE.md](AUDIO_PIPELINE.md))
- Акцент, шум, или нечеткая речь

**Решение:**
1. Переключитесь на base или small модель
2. Улучшите качество записи (говорите четче, ближе к микрофону)
3. Проверьте аудиопайплайн в [AUDIO_PIPELINE.md](AUDIO_PIPELINE.md)

### Проблема: Модель скачивается очень медленно

**Возможные причины:**
- Медленный интернет
- Блокировка HuggingFace в вашем регионе

**Решение:**
1. Используйте VPN если нужно
2. Скачайте с помощью торрент или альтернативного источника
3. Попробуйте загрузку отдельно, потом скопируйте файл

---

**Смотрите также:**
- [AUDIO_PIPELINE.md](AUDIO_PIPELINE.md) — Обработка аудио
- [event-flow.md](event-flow.md) — Поток событий
- [README.md](../README.md) — Основная документация
