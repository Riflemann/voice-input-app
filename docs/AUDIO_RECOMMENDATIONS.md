# Рекомендации по аудиоформатам и подготовке для распознавания (Whisper)

Этот документ содержит лучшие практики по выбору форматов аудио и настройке параметров для максимальной точности распознавания речи в текст.

## 📋 Содержание

1. [Форматы аудио](#форматы-аудио)
2. [Параметры записи](#параметры-записи)
3. [Обработка и подготовка](#обработка-и-подготовка)
4. [Реализация в voice-input-app](#реализация-в-voice-input-app)
5. [Практические рекомендации](#практические-рекомендации)

---

## Форматы аудио

### Форматы без потерь (рекомендуются)

| Формат | Описание | Когда использовать |
|--------|---------|-------------------|
| **WAV** (PCM) | Несжатый, высочайшее качество. Поддерживается всеми системами | Основной выбор для всех случаев, особенно речи |
| **FLAC** | Сжатие без потерь (~50% размер файла) | Если важна компактность без потери качества |
| **AIFF** | Похож на WAV, популярен на macOS | Для систем Mac |

### Форматы с потерями (осторожно)

| Формат | Параметры | Примечание |
|--------|-----------|-----------|
| **MP3** | Битрейт 192–320 кбит/с | Минимум 192 для речи, лучше 256+ |
| **AAC/Opus** | Соответствующие профили качества | HE-AAC v2, Opus 128 кбит/с |
| **OGG Vorbis** | Q6–Q10 (эквивалент 192–320 кбит/с) | Редко используется для речи |

### ❌ Форматы НЕ рекомендуемые

- **Очень низкие битрейты**: MP3 < 128 кбит/с, AAC < 96 кбит/с
- **Телефонные кодеки**: ULAW, ALAW (8 кбит/с) — потеря слишком много информации
- **Несколько перекодирований**: каждая ре-компрессия добавляет артефакты

---

## Параметры записи

### Частота дискретизации (Sample Rate)

| Частота | Качество | Размер | Рекомендация |
|---------|----------|--------|-------------|
| **16 кГц** | Телефонное | Минимум | ❌ Для Whisper — слишком низко |
| **44.1 кГц** | CD-качество | +100% к 22.05 | ⚠️ Работает, но потеря деталей |
| **48 кГц** | Профессиональное | +9% к 44.1 | ✅ **РЕКОМЕНДУЕТСЯ** |
| **96 кГц+** | Studio | x2 размер | Избыточно для речи |

**В voice-input-app**: Используем **48000 Hz** (по умолчанию) — оптимальный баланс качества и размера.

### Количество каналов

| Канал | Описание | Рекомендация |
|-------|---------|-------------|
| **Mono (1)** | Один микрофон | ✅ **ОПТИМАЛЬНО для речи** |
| **Stereo (2)** | Два микрофона | ⚠️ Требует синхронизации |
| **5.1, 7.1** | Многоканальные | ❌ Избыточно и усложняет обработку |

**В voice-input-app**: Захватываем **mono** (1 канал) — стандарт для распознавания речи.

### Глубина бита (Bit Depth)

| Глубина | Описание | Динамический диапазон |
|---------|---------|----------------------|
| **16-бит** | Стандартное качество | 96 дБ (CD-качество) |
| **24-бит** | Профессиональное | 144 дБ |
| **32-бит (Float)** | Обработка | Не имеет клиппинга |

**В voice-input-app**: Используем **32-бит Float (F32)** для обработки — позволяет безопасно работать с усилением без клиппинга.

---

## Обработка и подготовка

### Предотвращение артефактов при обработке

#### 1️⃣ Избегайте многократного перекодирования

```
ПЛОХО:  WAV → MP3 → AAC → WAV (каждый шаг добавляет потери)
ХОРОШО: WAV → обработка → F32 → одноразовое сохранение
```

**Реализация в коде**:
- Захватываем аудио в **F32** (без потерь)
- Конвертируем **I16/U16 → F32** в один проход
- Обрабатываем в F32
- Отправляем Whisper в F32

#### 2️⃣ Уровень громкости и предотвращение клиппинга

**Целевой RMS**: `0.12` (оптимален для Whisper, особенно русского языка)

```
RMS < 0.01    → очень тихо, увеличиваем gain
RMS = 0.12    → оптимально
RMS > 0.20    → риск клиппинга, снижаем gain
Peak > 0.95   → превышение, обрезание сигнала
```

**Формула адаптивного gain**:
```
gain = target_RMS / input_RMS
gain = clamp(gain, 0.5, 10.0)  // Ограничиваем диапазон

// Если peak * gain > 0.95 (пороговое значение)
max_gain = 0.95 / peak
gain = min(gain, max_gain)  // Предотвраняем клиппинг
```

#### 3️⃣ Мягкий Noise Gate

**Проблема**: жёсткий noise gate удаляет тихие согласные (ф, ч, ш, с, х)

**Решение**: мягкий noise gate с множителем `0.2` (оставляем 20% от тихих сэмплов)

```rust
if sample.abs() < noise_threshold {
    sample * soft_gate_factor  // 0.2 вместо полного удаления (0.0)
} else {
    sample  // Нормальный сэмпл
}
```

**Адаптивный threshold**:
```
noise_threshold = input_RMS * 0.15  // 15% от входного RMS
```

Это сохраняет фонемы, которые важны для распознавания.

#### 4️⃣ Использование F32 для обработки

**Преимущества Float32**:
- ✅ Нет клиппинга при обработке
- ✅ Высокая точность для адаптивных расчётов
- ✅ Стандарт для аудиообработки

**Конвертация без потерь (I16 → F32)**:
```rust
// Правильная нормализация
if sample < 0 {
    sample as f32 / 32768.0
} else {
    sample as f32 / 32767.0
}
```

---

## Реализация в voice-input-app

### Текущие настройки (обновлены)

**types.rs**:
```rust
sample_rate: 48000,          // Hz — выше 44100 для деталей
channels: 1,                 // Mono — оптимально для речи
peak_prevention_threshold: 0.95,  // Предотвращение клиппинга
soft_noise_gate_factor: 0.2,      // 20% для тихих сэмплов
```

**processor.rs**:
```rust
let target_rms = 0.12;  // Оптимален для русского языка

// Адаптивный gain
let adaptive_gain = target_rms / rms_input;
gain = adaptive_gain.clamp(0.5, 10.0);

// Предотвращение клиппинга
if peak_input * adaptive_gain > peak_threshold {
    let max_gain_for_peak = (peak_threshold / peak_input).max(0.5);
    gain = gain.min(max_gain_for_peak);
}

// Мягкий noise gate
noise_threshold = (rms_input * 0.15).min(0.01);
```

**capture.rs**:
```rust
// Конвертация без потерь (одноразовая)
let converted: Vec<f32> = data
    .iter()
    .map(|&sample| {
        if sample < 0 {
            sample as f32 / 32768.0  // I16 → F32
        } else {
            sample as f32 / 32767.0
        }
    })
    .collect();
```

### Интеграция с Whisper

Whisper требует:
- ✅ Фиксированная частота: **16000 Hz** (автоматически ресэмплируется)
- ✅ Mono канал
- ✅ Диапазон: [-1.0, 1.0]
- ✅ Формат: Float32

**Ресэмпливание (48 кГц → 16 кГц)**:
```rust
// Whisper автоматически ресэмплирует если нужно
let needs_resample = spec.sample_rate != 16000;
if needs_resample {
    // Whisper ресэмплирует без потерь
}
```

---

## Практические рекомендации

### Для разных сценариев

#### 📢 Интервью, Лекции, Подкасты

```
✅ Формат:    WAV 48 кГц, 16-бит, mono
✅ Микрофон:  Кондесаторный или динамический с поп-фильтром
✅ Помещение: Тихое, без эхо (мягкая мебель помогает)
✅ Уровень:   Говорящий на расстоянии 15–30 см от микрофона
✅ Проверка:  Запись должна быть слышна чётко, без искажений
```

#### 🎙️ Диктовка, Голосовой ввод

```
✅ Формат:    WAV 48 кГц, mono (потом автоматически обработается)
✅ Микрофон:  Встроенный в гарнитуру (наушники + микрофон)
✅ Помещение: Полусредний уровень шума приемлем
✅ Скорость:  Говорить ясно, избегать быстрой речи
```

#### 📱 Мобильное приложение

```
⚠️ Ограничение: Некоторые мобильные микрофоны работают на 44.1 или 22.05 кГц
✅ Решение:   Приложение автоматически ресэмплирует
✅ Формат:    Всё равно F32 для безопасной обработки
```

### Как проверить качество записи

1. **Визуальная инспекция** (например, в Audacity):
   - Волна должна заполнять примерно 50–80% от нормы
   - Пики НЕ должны касаться верхнего/нижнего края (клиппинг)
   - Без длинных молчаний в начале/конце

2. **Аудиошумомер**:
   - RMS > 0.05 для заметной речи
   - RMS = 0.12 идеально
   - RMS > 0.20 начинается риск

3. **Проверка шума**:
   - Запись молчания 3 сек в начале
   - Фоновый шум должен быть ≤ -30 дБ
   - Музыка/шум помещения ≤ -20 дБ

### Оптимизация микрофона и помещения

| Проблема | Решение |
|----------|---------|
| **Слишком тихо** | Приблизиться к микрофону, говорить громче, использовать поп-фильтр |
| **Фоновый шум** | Тихое помещение, дополнительная звукоизоляция, микрофон с кардиоидной диаграммой |
| **Эхо** | Мягкая мебель, занавески, ковры, стены из мягких материалов |
| **Взрывные звуки** | Поп-фильтр (3–5 см перед микрофоном) |
| **Клиппинг/искажение** | Снизить уровень громкости на микрофоне, отойти на 5–10 см дальше |

---

## Диагностика и логирование

### Логи обработки в voice-input-app

```
Processing audio: input RMS=0.087, peak=0.342
ADAPTIVE: Input RMS 0.087, peak 0.342 → gain 1.38 (target RMS: 0.12)
Output: RMS=0.12, Peak=0.47 (gain=1.38, threshold=0.013)
```

**Интерпретация**:
- ✅ Input RMS близко к целевому → обработка работает корректно
- ⚠️ Peak растёт после обработки → внимательнее с gain
- ✅ Threshold адаптируется → noise gate работает

### Общие проблемы

| Проблема | Логи | Решение |
|----------|------|---------|
| Очень тихая речь | Input RMS < 0.02 | Увеличить микрофон, говорить громче |
| Gain слишком большой | gain > 5.0 | Говорить ближе, тише по микрофону |
| Клиппинг | Peak > 0.95 после обработки | Снизить уровень микрофона |
| Много шума | Threshold = RMS * 0.15 растёт | Записывать в тихом месте |

---

## Резюме: Чек-лист для оптимальной записи

- ✅ **Формат**: WAV без потерь (F32 внутри, сохраняем как 16-бит или 24-бит)
- ✅ **Частота**: 48 кГц для захвата (Whisper ресэмплирует до 16 кГц)
- ✅ **Каналы**: Mono (1)
- ✅ **Уровень**: Целевой RMS = 0.12
- ✅ **Шум**: Не более 15% от входного RMS
- ✅ **Микрофон**: Кондесаторный с поп-фильтром, 15–30 см расстояния
- ✅ **Помещение**: Тихое, без эхо
- ✅ **Обработка**: Одноразовая конвертация, адаптивный gain, мягкий noise gate
- ✅ **Проверка**: Нет клиппинга, слышно чётко, без артефактов

---

**Дата последнего обновления**: Февраль 2026  
**Версия приложения**: voice-input-app с интегрированными рекомендациями
